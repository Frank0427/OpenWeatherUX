# Optional proxy used by future collectors. Never commit credentials or tokens.
HTTPS_PROXY=

# Future collection defaults. Region-specific values belong in config/regions.json.
HEADLESS=true
ARTIFACT_ROOT=data/raw
